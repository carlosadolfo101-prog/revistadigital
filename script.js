const pages=[...document.querySelectorAll('.page')];let current=0;let busy=false;const pageIndicator=document.getElementById('pageIndicator');const progressBar=document.getElementById('progressBar');const buttons={prev:[document.getElementById('prevBtn'),document.getElementById('prevSmall')],next:[document.getElementById('nextBtn'),document.getElementById('nextSmall')],first:document.getElementById('firstBtn'),last:document.getElementById('lastBtn')};function update(){pages.forEach((p,i)=>p.classList.toggle('active',i===current));pageIndicator.textContent=`Página ${current+1} de ${pages.length}`;progressBar.style.width=`${((current+1)/pages.length)*100}%`;buttons.prev.forEach(b=>b.disabled=current===0);buttons.next.forEach(b=>b.disabled=current===pages.length-1);buttons.first.disabled=current===0;buttons.last.disabled=current===pages.length-1;document.title=`${pages[current].dataset.title} | Héroes del Tiempo`}function turn(to){if(busy||to<0||to>=pages.length||to===current)return;busy=true;const old=current;const direction=to>old?'next':'prev';pages[old].classList.add(direction==='next'?'leaving-next':'leaving-prev');current=to;pages[current].classList.add('active');update();setTimeout(()=>{pages[old].classList.remove('leaving-next','leaving-prev','active');busy=false},720)}buttons.prev.forEach(b=>b.addEventListener('click',()=>turn(current-1)));buttons.next.forEach(b=>b.addEventListener('click',()=>turn(current+1)));buttons.first.addEventListener('click',()=>turn(0));buttons.last.addEventListener('click',()=>turn(pages.length-1));document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')turn(current+1);if(e.key==='ArrowLeft')turn(current-1);if(e.key==='Home')turn(0);if(e.key==='End')turn(pages.length-1)});let sx=0,sy=0;document.addEventListener('touchstart',e=>{sx=e.changedTouches[0].clientX;sy=e.changedTouches[0].clientY},{passive:true});document.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-sx;const dy=e.changedTouches[0].clientY-sy;if(Math.abs(dx)>50&&Math.abs(dx)>Math.abs(dy)){dx<0?turn(current+1):turn(current-1)}},{passive:true});update();